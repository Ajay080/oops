package Oops;

public class WrapperClasses {
    public static void main(String[] args){
        int a=10;
        Integer num=45; // It is passed by reference, not by value, 
        //or Integer num=new Integer(45);

        // int is a primitive data type, but we call also create it like object using wrapper 
        //classes like Integer, through which various methods comes in handy as well
    }
    /* Use of Final KeyWord */
    // in primitive data types: we can not change the value 
    // in non Primitive data types where we are creating objects the object, we can change there values, but we can not reassing again the reference variable
    
}
